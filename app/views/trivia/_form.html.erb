<%= form_for(@trivium) do |f| %>
  <% if @trivium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trivium.errors.count, "error") %> prohibited this trivium from being saved:</h2>

      <ul>
      <% @trivium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br />
    <%= f.text_field :question %>
  </div>
  <div class="field">
    <%= f.label :answer %><br />
    <%= f.text_field :answer %>
  </div>
  <div class="field">
    <%= f.label :coolness %><br />
    <%= f.number_field :coolness %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
